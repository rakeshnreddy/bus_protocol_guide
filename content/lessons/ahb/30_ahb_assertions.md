---
id: "30_ahb_assertions"
title: "AHB Assertions and Checkers"
summary: "Protocol-level properties that must be checked continuously."
protocol: "ahb"
tier: "1"
level: "expert"
order: 30
tags: ["ahb", "verification", "assertions", "sva"]
relatedLessons: []
prerequisites: ["29_ahb_simulation_strategy"]
visualIds: ["sig-ahb-assertion-library"]
exerciseIds: []
glossaryTerms: []
checklistIds: []
---

Simulation relies on checkers to automatically flag when a design violates the protocol. These are usually written in SystemVerilog Assertions (SVA) inside an interface monitor.

If any of these assertions fire during a test, the test must immediately fail, and the designer must fix the RTL.

## Critical Master Assertions

1. **Wait State Stability:** If a valid `NONSEQ` or `SEQ` transfer is pending while `HREADY` is 0, the master holds its address/control until completion. Write data for a stalled write data phase also remains stable. IDLE, BUSY, and the first ERROR cycle have defined exceptions, so the property needs transfer context.
2. **Burst Legality (HTRANS):** A burst starts with `NONSEQ`, and continuing data beats use `SEQ`; `BUSY` can defer a continuing beat. A fixed-length burst cannot end with `BUSY`, but an undefined-length `INCR` burst may terminate after `BUSY` with `IDLE` or `NONSEQ`.
3. **Burst Address Progression:** For each accepted `SEQ` beat in an incrementing burst, predict `HADDR` from the previous accepted beat plus the byte size specified by `HSIZE`. Do not advance the predictor on a wait state, `BUSY`, or `IDLE`.
4. **Wrap Boundary Enforcement:** For a `WRAP` burst, when the address reaches the calculated wrap boundary, the next `SEQ` address MUST wrap back to the lower boundary limit.

## Critical Slave Assertions

1. **Two-Cycle Error Protocol:** When the first ERROR cycle occurs with `HRESP = ERROR` and `HREADYOUT = 0`, the exact next cycle must keep `HRESP = ERROR` and drive `HREADYOUT = 1`. Any additional waits precede this pair with `HRESP = OKAY`.
2. **Zero-Wait IDLE/BUSY:** `IDLE` and `BUSY` receive a zero-wait `OKAY` response and must be ignored as data transfers.
3. **Exclusive Okay Legality:** When the optional Exclusive Transfers capability is present, `HEXOKAY` can only be asserted with `HREADY` and must not be asserted in the same cycle as `HRESP=ERROR`. The checker must also track the accepted exclusive-transfer context.

Open each checker to connect its trigger to its obligation and exception. A short assertion without the exception column is often worse than no assertion because it trains the team to waive real failures.

![Interactive AHB assertion library showing triggers, obligations, protocol exceptions, and DV failure signatures](visual:sig-ahb-assertion-library)

By building a robust library of these protocol assertions, you ensure that any stimulus generated by your constrained-random VIP will instantly catch bad behavior.
